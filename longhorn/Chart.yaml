apiVersion: v2
name: longhorn
description: Longhorn — distributed block storage on 75GB data disk
version: 1.0.0
dependencies:
  - name: longhorn
    version: "1.12.0"
    repository: "file://../reference-charts/longhorn"
