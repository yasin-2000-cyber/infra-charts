apiVersion: v2
name: ingress-nginx
description: NGINX Ingress Controller — TLS termination and routing
version: 1.0.0
dependencies:
  - name: ingress-nginx
    version: "4.15.1"
    repository: "file://../reference-charts/ingress-nginx"
