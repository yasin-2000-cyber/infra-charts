apiVersion: v2
name: mimir-distributed
description: Grafana Mimir — multi-tenant metrics hub (monolithic mode)
version: 1.0.0
dependencies:
  - name: mimir-distributed
    version: "5.5.0"
    repository: "https://grafana.github.io/helm-charts"
