apiVersion: v2
name: cert-manager
description: cert-manager — TLS certificate automation for all hub Ingress endpoints
version: 1.0.0
dependencies:
  - name: cert-manager
    version: "v1.17.2"
    repository: "oci://ghcr.io/cert-manager"
