apiVersion: v2
name: node-exporter
description: Prometheus node-exporter — node-level metrics for spine-test spoke
version: 1.0.0
dependencies:
  - name: prometheus-node-exporter
    version: "4.46.1"
    repository: "https://prometheus-community.github.io/helm-charts"
