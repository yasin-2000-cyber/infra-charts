kube-state-metrics:
  # Expose /metrics with scrape annotation so Alloy picks it up
  podAnnotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"

  resources:
    requests:
      cpu: 20m
      memory: 64Mi
    limits:
      cpu: 100m
      memory: 128Mi
