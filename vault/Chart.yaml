apiVersion: v2
name: vault
description: HashiCorp Vault — secret storage backend for all hub credentials
version: 1.0.0
dependencies:
  - name: vault
    version: "0.32.0"
    repository: "https://helm.releases.hashicorp.com"
