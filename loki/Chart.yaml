apiVersion: v2
name: loki
description: Grafana Loki — multi-tenant log storage hub (singleBinary mode, Longhorn backend)
version: 1.0.0
dependencies:
  - name: loki
    version: "6.6.2"
    repository: "https://grafana.github.io/helm-charts"
