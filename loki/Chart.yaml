apiVersion: v2
name: loki
description: Grafana Loki — multi-tenant log hub (singleBinary mode)
version: 1.0.0
dependencies:
  - name: loki
    version: "7.0.0"
    repository: "file://../reference-charts/loki"
