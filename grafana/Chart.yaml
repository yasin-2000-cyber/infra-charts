apiVersion: v2
name: grafana
description: Grafana — observability UI with Azure AD SSO
version: 1.0.0
dependencies:
  - name: grafana
    version: "10.5.15"
    repository: "file://../reference-charts/grafana"
